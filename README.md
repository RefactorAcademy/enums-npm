# What is this?

NPM package for getting the all enums

# Installation

`npm i npm-enum --save`

Then...

```
const enums = require("npm-enum")
enums.then(enums => enums.map((data) =>{
    console.log(data)
}))

```
